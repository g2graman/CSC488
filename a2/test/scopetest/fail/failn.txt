Fail
====

1) Tests the empty string.

2) Tests a statement embedded within a single nested scope that is not properly closed.

3) Tests a succession of two empty scopes each with one empty nested scope but with a variable declaration outside of the main scope.

4) Tests an even number of "begin" and "end" but where one of the "end" should end the loop. (Missing scope close)